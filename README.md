# NoteRAG
A retrieval-augmented generation (RAG) tool that grounds AI answers strictly in retrieved passages from your own notes, with inline citations — built to make the retrieve-then-generate pipeline visible.
